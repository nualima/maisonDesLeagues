{% extends "pageLayout.twig" %}
{% block body %}
	<body>

		<div id="user">{# {{user.name ?
													 "{{user.name}}" 
													 : 
													 
													 }} #}




		</div>
		<!-- noir avec opacité moins forte en background -->


		
		{% if users.lo	gin is defined %}

			<div class="text-white mt-10">
			<h1 class="font-bold text-4xl">Bonjour, {{users.login}}</h1>
		</div>
			{% include './component/disconnect.twig' with {'users': users} only %}

		{% else %}
		

<a href="./?controller=Home&action=connection" type="button" class="btn  bottom-12 right-12 w-500 h-500 	 flex mr-3 text-sm bg-blue-800 rounded-full md:mr-0 focus:ring-4 focus:ring-gray-300 dark:focus:ring-gray-600" id="user-menu-button" aria-expanded="false" data-dropdown-toggle="dropdown">

<p class="text-white text-2xl mx-4">Connection</p>
<p>
</a>
		{% endif %}
	{% endblock %}
