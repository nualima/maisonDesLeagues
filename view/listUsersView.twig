{% extends "pageLayout.twig" %}
{% block body %}
<div>
</div>

			{% include './component/usersTable.twig' with {'users': users} only %}

	{# {% for user in users %}
		<table>

			<td>{{ user.username|e }}</td>
            <td>{{user.login}}</td>
            

			<td><a href="?controller=users&action=delete&id='.$users->getId() . '" class="text-danger">x</a></td>
						
			<td><a href="?controller=users&action=edit&id='.$users->getId() . '">'modifier '</a></td>
			

		</table>

	{% endfor %} #}
{% endblock %}
